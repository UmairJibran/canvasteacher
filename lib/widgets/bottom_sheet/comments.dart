import 'package:flutter/material.dart';

class BottomSheetCommentTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("COMMENTS")],
    );
  }
}
